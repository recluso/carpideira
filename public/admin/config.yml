backend:
  name: github
  repo: recluso/carpideira 
  branch: main
  # --- Add these two lines ---
  auth_endpoint: /api/auth 
  base_url: https://carpideira.com # Important: Use your actual production URL

# The publish mode (allows for draft pull requests before publishing)
publish_mode: editorial_workflow

media_folder: "public/img/uploads" # Where images will be saved
public_folder: "/img/uploads" # How images are referenced in posts

collections:
  - name: "posts"
    label: "Posts"
    folder: "content/blog" # Adjust based on where posts live in the Eleventy starter
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Draft", name: "draft", widget: "boolean", default: true, required: false}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "featured_image", widget: "image", required: false}
      - {label: "Body", name: "body", widget: "markdown"}