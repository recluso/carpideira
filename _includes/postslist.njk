<ol reversed class="postlist" style="--postlist-index: {{ (postslistCounter or postslist.length) + 1 }}">
{%- for post in postslist | reverse %}
	<li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
        
        {# 💡 CRITICAL FIX: Ensure featured_image is a string before any check #}
        {% set featureImage = post.data.featured_image | default('') %}

        {# Now, check the length of the *clean* string #}
        {% if featureImage | trim | length > 0 %}
            <a href="{{ post.url }}" class="postlist-thumb-link">
                {# Use the cleaned variable 'featureImage' #}
                {% imageThumbnail featureImage, post.data.title | default(post.url) %}
            </a>
        {% endif %}
        
		<a href="{{ post.url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
		<time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("LLLL yyyy") }}</time>
	</li>
{%- endfor %}
</ol>